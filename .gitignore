# dependencies
node_modules

# next.js build output
.next/
out/
build/

# env / secrets
.env
.env.*

# local working media — source clips, keyframes, raw stills (not shipped)
_local/
keyframes/
dt-out/

# guard: raw/intermediate render clips should never be committed
public/video/op-*.mp4
